// Comments

fn main() {
    // This is an example of a line comment
    // There are two slashes at the beginning of the line
    // and nothing written inside these will be read by the compiler
     
    // println!("Hello World!")

    /*
     * This is another type of coment, a block comment. In general,
     * line comments are the recommended comment style. But block 
     * comments are extremely useful for temporarily diabling chunks of code.
     * It takes only a few keystrokes to comment out everything.
     */

    // Note: the previous column of '*' was entirely for style.
    // There is no need for it.
    
    let x = 5 +  90+  5;
    println!("Is 'x' 10 or 100? x = {}", x);

